// TODO: Look for mediaconvert and transcribe events and send callback when job is done
export const handler = async () => {
    // Search for service token in db
    // Send callback
    return {
        status: 200
    }
}
