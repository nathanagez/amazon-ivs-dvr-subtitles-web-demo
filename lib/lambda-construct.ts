import {Construct} from "constructs";

export class LambdaConstruct extends Construct {
    constructor(scope: Construct, id: string) {
        super(scope, id);
        // TODO: create and expose all Lambda Functions here
    }
}
